# login-page

> Generated by **AgentOS** on 2 May 2026  
> Quality Score: **90/100** (Grade **A**) · Confidence: **83/100**

## Overview

A clean, accessible sign-in page with email/password authentication, form validation, and optional social login support.

**Theme:** Light theme (customizable via CSS variables in `styles/main.css`)

## Features

- Fully responsive (mobile-first, 375px → 1440px)
- Light theme with design system tokens
- Smooth CSS animations and transitions

## Tech Stack

- **HTML5**
- **CSS3 (Custom Properties)**
- **Vanilla JS**
- **Express.js** (local dev server)
- **dotenv** (environment config)

## Folder Structure

```
project/
├── index.html          # Main HTML entry point
├── styles/
│   └── main.css        # All styles (design system + page-specific)
├── scripts/
│   └── main.js         # JavaScript logic and interactivity
├── server.js           # Local dev server (Express static)
├── package.json        # Node.js project manifest
├── .env                # Environment variables (copy from .env.example)
├── .env.example        # Example environment config
└── README.md           # This file
```

## Prerequisites

Make sure you have the following installed:

- **Node.js** v18 or higher → [Download](https://nodejs.org/)
- **npm** v9+ (comes with Node.js)

Verify with:
```bash
node --version   # should print v18.x.x or higher
npm --version    # should print 9.x.x or higher
```

## Installation

### 1. Clone or download the project

If you downloaded a ZIP file, extract it to a folder of your choice.

### 2. Navigate into the project folder

```bash
cd login-page
```

### 3. Install dependencies

```bash
npm install
```

### 4. Configure environment variables

```bash
cp .env.example .env
```

Then open `.env` and fill in your values:

```env
# Server configuration
PORT=3000

# Authentication (add your backend details here)
# API_BASE_URL=http://localhost:5000/api
# JWT_SECRET=your_jwt_secret_here
```

## Running the Project

### Start the local server

```bash
npm start
```

Then open your browser at:

```
http://localhost:3000
```

### Development mode (auto-restart on file changes)

```bash
npm run dev
```

> Requires `nodemon`. It will be installed automatically with `npm install`.

### Open directly (no server needed)

You can also open `index.html` directly in your browser — no server required for basic viewing:

```bash
# macOS
open index.html

# Linux
xdg-open index.html

# Windows
start index.html
```

## Usage & Customization

### Form Submission
The login form submits to `/api/auth/login` by default. Customize the `action` or JS handler in `scripts/main.js`:
```js
// scripts/main.js
document.querySelector('form').addEventListener('submit', async (e) => {
  e.preventDefault();
  const email = document.querySelector('#email').value;
  const password = document.querySelector('#password').value;
  // Replace with your actual API call:
  const res = await fetch(process.env.API_BASE_URL + '/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email, password })
  });
});
```

## Environment Variables Reference

| Variable | Default | Description |
|----------|---------|-------------|
| `PORT` | `3000` | Port the local server listens on |

Add additional variables to `.env` as needed for your backend integration.

## Customizing Styles

All design tokens are CSS custom properties defined at the top of `styles/main.css`:

```css
:root {
  --color-primary: #6366f1;
  --color-bg: #ffffff;
  --color-text: #0f172a;
  --border-radius: 0.5rem;
  /* ... more tokens */
}
```

Change any value here and it propagates across the entire page automatically.

## Quality Report

| Metric | Score |
|--------|-------|
| Validation Score | 90/100 |
| Grade | A |
| Confidence Score | 83/100 |
| Responsive | ✅ Yes |
| Accessible | ✅ Yes |

## License

MIT — free to use, modify, and distribute.

---

*Built with [AgentOS](https://github.com/agentOS) — AI-powered web generation*
